<?xml version="1.0" encoding="UTF-8"?>
<addon id="vfs.stream.fast"
       version="1.4.0.0"
       name="VFS Stream Fast"
       provider-name="forbxy">
  
  <requires>
    <import addon="kodi.binary.global.main" version="2.0.3"/>
    <import addon="kodi.binary.instance.vfs" version="3.0.1"/>
  </requires>

  <!-- 
    核心配置：
    protocols: 我们接管 dav, davs (WebDAV) 以及 http, https
    files="true": 我们负责文件读取（这是我们加速的地方）
    directories="false": 我们不负责目录列表（让 Kodi 内部的 CDAVDirectory 继续工作，省去了解析 XML 的麻烦）
  -->
  <extension point="kodi.vfs"
             files="true"
             directories="false"
             supportWrite="true"
             protocols="dav|davs|http|https"
             library_windows="vfs.stream.fast.dll"
             library_linux="libvfs.stream.fast.so"
             library_android="libvfs.stream.fast.so"
             library_osx="vfs.stream.fast.dylib"/>

  <extension point="xbmc.addon.metadata">
    <platform>all</platform>
    <summary lang="en_GB">High-performance HTTP/WebDAV VFS implementation</summary>
    <summary lang="zh_CN">高性能http/webdav vfs实现</summary>
    <description lang="en_GB">Homepage: github.com/forbxy[CR]Features:[CR]1. LRU block cache: optimizes network ISO movie parsing, playback starts in seconds[CR]2. Read-ahead buffer: with default settings, 10s+ network fluctuations are imperceptible[CR]- For STRM usage, see STRM format requirements on the homepage vfs.stream.fast[CR]- For Emby integration, emby-next-gen requires a modified version, see homepage plugin.service.emby-next-gen</description>
    <description lang="zh_CN">主页:github.com/forbxy[CR]特点:[CR]1.LRU块缓存：优化网络iso电影解析速度，起播只需数秒[CR]2.预读数据：默认参数下，10秒+的网络波动用户无感知[CR]-！如需使用strm，对STRM文件的格式要求见主页vfs.stram.fast[CR]-！如需接入emby，emby-next-gen只能使用修改版，具体见主页plugin.service.emby-next-gen</description>
    <assets>
      <icon>icon.png</icon>
    </assets>
  </extension>
</addon>
